export const metadata = {
  title: "Hyper Store7",
  description: "Cursos online com acesso rápido",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
